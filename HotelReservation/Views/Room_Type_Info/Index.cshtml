@model IEnumerable<HotelReservation.Models.ROOM_TYPE_INFO>

@{
    ViewBag.Title = "Index";
}

<h2>Room Type Informations</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ROOM_TYPE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ROOM_PRICE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DESCRIPTION)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MAX_GUEST)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DISCOUNT)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ROOM_TYPE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ROOM_PRICE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DESCRIPTION)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MAX_GUEST)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DISCOUNT)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ROOM_TYPE }) |
            @Html.ActionLink("Details", "Details", new { id=item.ROOM_TYPE }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ROOM_TYPE })
        </td>
    </tr>
}

</table>
