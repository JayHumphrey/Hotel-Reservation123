@model IEnumerable<HotelReservation.Models.ROOM_DETAILS>

@{
    ViewBag.Title = "Index";
}

<h2>Room Details</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ROOM_TYPE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TOTAL_ROOMS)
        </th>
        
        <th>
            @Html.DisplayNameFor(model => model.ROOM_TYPE_INFO.DESCRIPTION)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.STATUS)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ROOM_TYPE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TOTAL_ROOMS)
        </td>
        
        <td>
            @Html.DisplayFor(modelItem => item.ROOM_TYPE_INFO.DESCRIPTION)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.STATUS)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ROOM_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ROOM_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ROOM_ID })
        </td>
    </tr>
}

</table>
